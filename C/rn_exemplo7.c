main(){
	float preco = 15.99;  // inicializar variaveis
	printf("O produto custa R$ %f \n", preco);
	
	printf("Com duas casas = %.2f", preco); 
	// define as casas decimais
	
	printf("Com duas casas = %8.2f", preco); 
	// reserva 8 espaços no texto e define 2 casas decimais
}

// use float para numeros REAIS (com casas decimais)
