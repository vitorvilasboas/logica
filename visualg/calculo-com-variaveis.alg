algoritmo "calculo com variaveis"
var
v1, v2 : real

inicio
escreval("Digite dois valores: ")
leia(v1, v2)

escreval("Soma = ", v1 + v2)
escreval("Subtração = ", v1 - v2)
escreval("Multiplicação = ", v1 * v2)
escreval("Divisão = ", v1 / v2)

fimalgoritmo
