algoritmo "enquanto-q4"
var
x, y, soma : inteiro

inicio

soma <- 0

enquanto (soma <= 15) faca
  escreval("Digite 2 valores: ")
  leia(x, y)
  soma <- x + y
  escreval("Soma = ", soma)
fimenquanto

fimalgoritmo
